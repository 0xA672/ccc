// ccc unit tests — run with: c3c test
module ccc_test;
import ccc;
import std::testing;

fn void test_language_detection() @test
{
    // These are integration-level; unit tests for core functions
    testing::pass();
}
